/**
 * Custom JS.
 *
 * Custom JS scripts.
 *
 * @since 1.0.0
 */
jQuery(document).ready(function($) {
		 
	var nav = responsiveNav(".nav-collapse", { 
	  customToggle: "#menu-toggle"
	});
			    
});